apiVersion: apps/v1
kind: Deployment
metadata:
  name: aws-dummy-backend
  namespace: platform-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aws-dummy-backend
  template:
    metadata:
      labels:
        app: aws-dummy-backend
    spec:
      containers:
        - name: dummy-backend
          #image: 'asia-south1-docker.pkg.dev/plutos-platform/plateform-dev/cams-backend:latest'
          image: '061039767119.dkr.ecr.ap-south-1.amazonaws.com/platform-uat-cams-be:latest'  
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          # resources:
          #   requests:
          #     memory: "512Mi"
          #     cpu: "250m"
          #   limits:
          #     memory: "1024Mi"
          #     cpu: "500m"      
          # envFrom:
          #   - configMapRef:
          #       name: ums-config
          # env:
          #   - name: REDIS_HOST
          #     valueFrom:
          #       configMapKeyRef:
          #         name: ums-config
          #         key: REDIS_HOST
          #   - name: REDIS_PORT
          #     valueFrom:
          #       configMapKeyRef:
          #         name: ums-config
          #         key: REDIS_PORT
          # livenessProbe:
          #   httpGet:
          #     path: /health  # Adjust the path
